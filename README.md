# 2048

Clone the project:
```bash
git clone https://github.com/char-projects/2048.git
```

To compile it run this in your terminal:
```bash
python3 -m http.server 8000
```

Then click on the link in your terminal (should be something like this: http://0.0.0.0:8000/)